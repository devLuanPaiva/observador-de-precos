spring:
  application:
    name: observador-de-precos-api

  datasource:
    url: jdbc:postgresql://localhost:5432/appdb
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  security:
    user:
      name: dev_luanpaiva
      password: 565a2051-676b-4baf-bf51-2a0465b97a14
